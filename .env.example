# Secrets only — copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# AI provider is chosen on the command line (not here):
#   python main.py --ai --provider anthropic
#   python main.py --ai --provider openai

# Optional: override app.env model IDs without editing app.env
# ANTHROPIC_MODEL_DEEP=claude-sonnet-4-6
# ANTHROPIC_MODEL_FAST=claude-haiku-4-5

# Telegram (GitHub Actions secrets + local .env)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# TELEGRAM_CAPTION=Indian Market Advisory — daily report
