AI_PROVIDER=openai
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Market data uses yfinance (Yahoo) with jugaad-data (NSE) fallback — no API keys required.

# Telegram (GitHub Actions secrets + local .env) — from @BotFather + your chat id
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# TELEGRAM_CAPTION=Indian Market Advisory — daily report
